# AFAC2026 Task2 AGENTS

本文件适用于当前仓库根目录及其子目录。优先级高于默认工作习惯，供后续 Codex/agent 工作时遵循。

## 语言与沟通

- 默认使用中文回复。
- 说明问题时优先给出可执行结论，不要空泛描述。
- 涉及文件时，尽量使用绝对路径引用。

## 项目边界

- 本项目是“Task2 复杂金融文档还原挑战”。
- 只允许使用赛题允许的 FinixDoc-VL 作为大模型/VLM 接口。
- 不要接入其他外部大模型 API。
- `data/` 目录视为只读输入，不要修改、重命名或覆盖其中原始数据。
- `.env` 可能包含 `apiKey` 和 `userIds`，不得打印、提交或写入日志。

## 工作方式

- 先读文档再改代码，重点看：
  - `docs/Task2复杂金融文档还原挑战.md`
  - `docs/FinixDoc-VL的API调用说明.md`
  - `docs/技术方案-gpt5.5thinking/04_技术架构文档.md`
  - `docs/技术方案-gpt5.5thinking/05_实施清单与风险控制.md`
- 若存在 `docs/superpowers/plans/*.md` 计划文件，优先按计划执行。
- 改动要尽量小而集中，避免顺手重构无关模块。
- 不要回滚你没改过的用户变更。

## 代码与文件约定

- 新增代码默认使用 ASCII。
- 中文注释只在规则复杂、容易误解的地方补充。
- 代码应模块化，按职责拆分文件。
- 不要把提交结果、缓存、日志写死到固定路径；优先使用运行配置或 `work_dir`。
- 不要硬编码测试集文件名、提交内容或榜单结果。

## 验证要求

- 修改代码后，优先运行相关单测。
- 触及提交输出时，至少校验：
  - CSV 可被正常读取
  - 列名正确
  - 行数正确
  - 没有重复文件名
- 涉及 API 调用时，确保日志和缓存不泄露密钥。

## 产物目录

- `outputs/` 只放运行产物，不提交进仓库。
- `docs/` 放说明、计划、复现和审计类文档。
- `tests/` 放可重复执行的测试。

## 禁止事项

- 不要输出或提交真实密钥。
- 不要引入未授权的模型/API。
- 不要修改 `data/` 下原始样本。
- 不要用测试集文件名做特判逻辑。
- 不要在计划文档里塞大段完整实现。

## 建议命令

```bash
git status --short
git diff --check
pytest -q
```

如涉及提交生成，再额外检查输出 CSV 是否满足赛题格式。
